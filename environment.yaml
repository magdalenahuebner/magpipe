name: magpipe 
channels:
  - conda-forge
  - bioconda
  - defaults
dependencies:
  - python=3
  - snakemake
  - pandas
  - numpy
  - ipykernel
  - matplotlib
  - requests
  - scipy
  - scikit-learn
  - tqdm
  - r-reshape2
  - r-plyr
  - bioconductor-limma
  - pip
  - pip:
    - uniprot-id-mapper==1.1.1
    - -e .   # make src modules available locally
